export default function BackendStudentenDashboard() {
    return (
        <section className="section">
            <h1>Villa Vredestein – Studenten Dashboard Backend</h1>
            <p>Spring Boot backend met JWT-auth, API en database-koppeling.</p>
        </section>
    );
}